- read each line and translate commands (each line is a new command)
- run each command in sequence
- commands:
- each command has command character, then white spaced params eg "I M N" or "H X1, X2, Y, C"
- command characters "I", "C", "L", "V", "H", "S"
- "I M N" = new matrix with "M" rows and "N" columns
- draw the matrix with blank values
- fill the matrix with all white
- clear the matrix
- add horizontal color
- add vertical color
- validate input commands
